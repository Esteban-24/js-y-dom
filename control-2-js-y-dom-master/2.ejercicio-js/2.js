'use strict';
/*const reloj = document.querySelector('h1');

function format(num) {
    if (num<10) {
        return '0' + num;
    }
    return num;
}
const now = new Date();
function tiempo(){
    const dia = now.getDate()
    const horas = format(now.getHours());
    const min = format(now.getMinutes());
    const seg = format(now.getSeconds());

    reloj.textContent = `${horas}:${min}:${seg}`;
    
}
setInterval(tiempo, 1000);
tiempo()
*/